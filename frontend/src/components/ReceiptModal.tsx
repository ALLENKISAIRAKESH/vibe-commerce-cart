import React from 'react'; export default function ReceiptModal({ receipt, onClose }){ return (<div className='modal-backdrop' onClick={onClose}><div className='modal' onClick={(e)=>e.stopPropagation()}><h3>Receipt</h3><p><strong>Name:</strong> {receipt.name}</p><p><strong>Email:</strong> {receipt.email}</p><p><strong>Total:</strong> ₹{receipt.total.toFixed(2)}</p><p><strong>Timestamp:</strong> {new Date(receipt.timestamp).toLocaleString()}</p><div className='modal-items'>{receipt.items.map(it=> <div key={it.id} className='modal-row'><span>{it.name} x{it.qty}</span><span>₹{(it.price*it.qty).toFixed(2)}</span></div>)}</div><button onClick={onClose}>Close</button></div></div>) }